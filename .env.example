# =============================================================================
# EMKAI DASHBOARD - ENVIRONMENT CONFIGURATION
# =============================================================================
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit .env with real credentials to version control
# =============================================================================

# Supabase Configuration (VITE_ prefix required for frontend)
# Single database with multi-tenant isolation via client_id
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Service Role Key (backend only - DO NOT expose to frontend)
# WARNING: This key bypasses RLS - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# N8N Webhook Configuration
# Base URL for webhooks - will be combined with client_id
# Format: {base_url}/{type}-{client_id} (e.g., whatsapp-eb024fe2-... or email-eb024fe2-...)
VITE_N8N_WEBHOOK_BASE_URL=https://your-n8n-instance.com/webhook-test

# Backend API Configuration
# VITE_API_URL: For calendar server (port 3001)
VITE_API_URL=http://localhost:3001

# VITE_API_BASE_URL: For saas-backend authentication API (port 3000)
VITE_API_BASE_URL=http://localhost:3000

# =============================================================================
# SECURITY KEYS (Backend only - generate strong random values)
# =============================================================================
# JWT secret for calendar server authentication
JWT_SECRET=your-jwt-secret-minimum-32-characters

# HMAC secret for OAuth state signing
STATE_SECRET=your-state-secret-minimum-32-characters

# AES-256 encryption key for token storage (64 hex characters = 32 bytes)
TOKEN_ENCRYPTION_KEY=your-64-character-hex-encryption-key-here
